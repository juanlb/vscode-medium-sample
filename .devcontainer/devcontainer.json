{
    "name": "Sample App",
    "dockerComposeFile": "../docker-compose.yml",
    "service": "web",
    "workspaceFolder": "/app",
    "shutdownAction": "stopCompose",
    "remoteEnv": { "SHELL": "${localEnv:SHELL}" }
}